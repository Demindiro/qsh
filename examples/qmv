#!/usr/bin/env qsh

find . -name *.rs >

if @?; print "success (find)"

print Rust source files:
print @

grep < runtime >

if @?; print "success (grep)"

print Runtime source files:
print @

split <

split < >

print splitted: @

for a in @
	if test -n @a
		print ">" @a
